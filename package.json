{
    "name": "jam54",
    "browserslist": [
        "> 0.5%",
        "last 2 versions",
        "not dead"
    ],
    "version": "1.0.10",
    "description": "",
    "keywords": "",
    "license": "LGPL-3.0-only",
    "author": "jam54",
    "scripts": {
        "build": "rimraf ./bin && parcel build index.html --public-url ./ --dist-dir ./bin",
        "dev": "rimraf ./dist && parcel index.html --dist-dir ./dist --open",
        "start": "npm run build && npm run dev",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "postcss": "^8.4.24",
        "tailwindcss": "^3.3.2"
    },
    "devDependencies": {
        "@parcel/packager-raw-url": "^2.9.0",
        "@parcel/transformer-webmanifest": "^2.9.0",
        "parcel": "^2.9.1",
        "rimraf": "^5.0.1"
    }
}
